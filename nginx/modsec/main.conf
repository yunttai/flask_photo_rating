# OWASP CRS 로드
Include "/etc/modsecurity/crs/crs-setup.conf"
Include "/usr/share/modsecurity-crs/owasp-crs.load"
# 임계치(처음은 탐지 전용 → 안정화 후 차단 On)
SecAction "id:900110,phase:1,nolog,pass,setvar:tx.anomaly_score_blocking=on,setvar:tx.inbound_anomaly_score_threshold=5,setvar:tx.outbound_anomaly_score_threshold=4"
# 필요시 예외/화이트리스트 추가 지점
# SecRuleRemoveById 942100
